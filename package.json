{
  "name": "lumia-autofix",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host localhost --port 4173"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
